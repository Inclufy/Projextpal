# Sentry error reporting (optional)
# EXPO_PUBLIC_SENTRY_DSN=https://your-key@sentry.io/your-project-id

# docker-compose.yml (dev) reads this for the Postgres service — required.
# Set a real value in a local .env (gitignored); never commit it.
POSTGRES_PASSWORD=
