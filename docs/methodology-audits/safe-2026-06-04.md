# SAFe Alignment Report — 2026-06-04

**Module:** `backend/safe/` (4 models, 0 `@action`s) · **FK target confirmed:** `programs.Program` (`backend/safe/models.py:10`, `:51`) — NOT `program_management/`.
**Headline verdict:** ❌ No executable PI loop. The module is **four flat record-stores** behind a sidebar that promises a full SAFe experience but renders one placeholder component for every route. A portfolio with no operable PI is theatre — this is theatre.

## Fidelity by layer

| Layer | Score | Δ vs baseline |
|---|---|---|
| Portfolio (LPM/WSJF) | 0% | unchanged — no value streams, no epics, no WSJF |
| Program (ART/PI) | 30% | unchanged — ART + PI + PIObjective stored, none enforced |
| Team linkage | 5% | unchanged — `team` is a free-text `CharField`, no FK |
| Execution / delivery (ART/PI-walk) | 8% | unchanged — no operable PI loop |

**Weighted overall fidelity ≈ 14%.**

## Construct violations (highest priority)

1. ❌ **No executable PI loop.** All 7 SAFe sidebar routes (`art`, `pi/current`, `pi/planning`, `pi/objectives`, `features`, `demos`, `inspect-adapt`) render the **same generic `ProgramDashboard`** placeholder — `frontend/src/App.tsx:633-667`. The component itself documents this: "Until each gets its own dedicated [page]" — `frontend/src/pages/ProgramDashboard.tsx:285-289`. It shows a localized title/subtitle only; it makes **zero** calls to the `safe/` API. The 4 viewsets (`backend/safe/views.py`) have **no frontend consumer at all**.
2. ❌ **No Features / Capabilities.** No model; backlog cannot decompose to stories. The sidebar "Features" link (`AppSidebar.tsx:70`) is a dead shell. Only the 4 baseline models exist (`backend/safe/migrations/0001_initial.py` — 4 `CreateModel`).
3. ❌ **No real team linkage.** `AgileReleaseTrain.team_count` is a bare `IntegerField` (`models.py:13`) and `PIObjective.team` is a free-text `CharField` (`models.py:83`). No FK to `programs.ProgramTeam` (`programs/models.py:136`). ARTs do not group real teams.
4. ❌ **No dependency / program board.** `ARTSync.impediments`/`decisions` are untyped `JSONField` blobs (`models.py:34-35`); nothing is ROAMed, nothing links cross-team dependencies.
5. ❌ **No WSJF.** No Cost-of-Delay, job-size, or score fields anywhere. Backlog ordering is `-business_value` only (`models.py:89`).
6. ⚠️ **PI Objective business value = stored, not enforced.** `business_value` exists (`models.py:80`) but `committed`/`achieved` are free booleans (`:81-82`); no commitment ceremony, no actual-vs-planned BV roll-up, no link to features.
7. ❌ **No System Demo & no Inspect & Adapt models.** Sidebar links exist (`AppSidebar.tsx:78-79`); no backing model or page.
8. ❌ **No Lean Portfolio / value streams.** Portfolio layer entirely absent.

## SAFe-level matrix (Direction A — governance fidelity)

| Construct | Status | Evidence |
|---|---|---|
| Value streams + Portfolio epics | ❌ absent | no model |
| WSJF prioritization | ❌ absent | no CoD/job-size fields |
| Agile Release Train | ⚠️ stored | `models.py:6` — has `rte` FK ✅, but `team_count` int not real teams |
| Program Increment | ⚠️ stored | `models.py:47` — timebox + status + iteration_count, no iterations modelled |
| PI Planning event | ❌ absent | only a `pi_planning_date` date field (`models.py:61`); no event flow |
| Features → Stories | ❌ absent | no Feature model |
| ART Sync | ⚠️ stored | `models.py:27` — JSON blobs, no dependency/risk typing |
| System Demo | ❌ absent | no model |
| Inspect & Adapt | ❌ absent | no model |

## ART execution-walk matrix (Direction B — operability)

| PI | Plan | Decompose | Sync | Roll-up | Demo | I&A | Blocked role |
|---|---|---|---|---|---|---|---|
| Any PI | ⚠️ objectives storable via API, no UI/ceremony | ❌ no features/stories | ⚠️ JSON note only | ❌ no state roll-up | ❌ | ❌ | **RTE** (cannot run PI Planning, no board, no demo); **Team** (cannot pull features→stories, no FK) |

**Operability ≈ 8%** — an RTE cannot run a Program Increment end-to-end; a team has nothing to pull. The API can persist isolated PI/objective rows, but no frontend surfaces them and nothing rolls up.

## Feature proposals + redesign (ranked — both directions)

1. **[FLAGSHIP redesign — executable PI loop]** Add `Feature` (FK `program_increment`, `art`, `wsjf_*`, state) + `Story` (FK `feature`, FK `programs.ProgramTeam`) + `Dependency` (FK source/target feature, ROAM status) + `SystemDemo` + `InspectAdapt` (improvement items). Wire `PIObjective.team` → FK `ProgramTeam`; add BV roll-up (planned vs achieved) on `ProgramIncrement`. Build real pages for the 7 sidebar routes (PI Planning board, Features kanban, Program Board, System Demo, I&A) replacing the shared `ProgramDashboard` shell. *weight × value × effort: highest.*
2. **[WSJF prioritization]** `wsjf_score = (business_value + time_criticality + risk_reduction) / job_size` on `Feature`; order the program backlog by it. *high value, low effort once Feature exists.*
3. **[Real ART↔Team linkage]** M2M/FK `AgileReleaseTrain.teams → programs.ProgramTeam`; drop `team_count`. *medium.*
4. **[Lean Portfolio]** `ValueStream` + `PortfolioEpic` (lean business case, guardrails) above the ART. *high value, higher effort — defer behind PI loop.*

> ⚠️ Any of the above requires new models + `makemigrations safe` → **data-guardian backup is mandatory** (CLAUDE.md §9) before migrating. This report is read-only; nothing was scaffolded or migrated.

## Drift since baseline

**Zero structural drift.** Baseline (4 models, 0 actions, flat stores) is fully confirmed. The only thing newer than the baseline is the **frontend over-promise**: `AppSidebar.tsx:50-91` advertises a complete SAFe IA (ART, PI Planning, Features, System Demo, I&A) and `App.tsx:633-667` registers all 7 routes — but every route resolves to the placeholder `ProgramDashboard`. The gap between advertised IA and backing implementation **widened**, raising user-perceived-vs-real risk.
