# Methodology Suite — Prioritized Implementation Backlog

**Date:** 2026-06-04 · **Source:** the 13 Direction-C proposals in `docs/methodology-audits/*.md` + `SUMMARY-2026-06-04.md`.
**Legend:** 🔒 = adds a model → `makemigrations` → **mandatory data-guardian backup before applying** (CLAUDE.md §9; live tenant `zanjabil@inclufy.com` + Yanmar seed). · Effort: **S** ≤½d, **M** 1–2d, **L** 3d+. · Each ticket: *file:line → change → acceptance criteria (AC)*.

Priority bands:
- **P0** — credibility / foundational (a course teaches it, or other modules depend on it).
- **P1** — high weight × low effort, mostly **no migration** (enforce data already stored).
- **P2** — structural redesign (new models, larger flows).

---

## P0 — Credibility & foundational

### P0-1 · Governance: Decision → component authorization 🔒 · M
The single change that turns the governance layer from theatre into governance; PMI / P2-Programme / PRINCE2 boards all build on it.
- `governance/models.py:157-220` → add `outcome` (`authorize`/`continue`/`hold`/`stop`), nullable target FKs (`authorized_project`/`authorized_program`/`authorized_portfolio` **or** a generic `target_content_type`/`target_object_id`), `applied_at`.
- `governance/views.py` (DecisionViewSet `:299`) → add `@action def apply` that, on transition to `approved`, sets the target component `status` (`active`/`on_hold`/`closed`) and writes an immutable audit row.
- Frontend `Decisions.tsx` → "Apply decision" action + show the linked component + its new state.
- **AC:** approving a `hold` Decision on a Program flips that Program to `on_hold`; the Decision becomes append-only once applied; an audit row is written; rejecting changes nothing. Cross-tenant apply denied.

### P0-2 · Governance: MeetingAction lifecycle 🔒 · S
Minutes today are dead text (`Decisions.tsx:405` even advertises an "auditable trail" with no consequence).
- `governance/models.py:223-275` → new `MeetingAction(meeting FK, description, owner FK, due_date, status[open/in_progress/done/cancelled], closed_at)`, `related_name='governance_meeting_actions'`, `Meta.ordering`.
- Frontend `BoardDetail.tsx` / `ProgramGovernance.tsx:146-186` → tracked-actions panel with overdue badges.
- **AC:** a meeting action persists with owner + due date; overdue actions are flagged; closing sets `closed_at`.

### P0-3 · SAFe: badge the course OR build the PI loop — decision ticket · S (decision) / L (build)
A full 16h `safeCourse` (PI Planning/ART/System Demo/I&A) sits over a 14%-fidelity product where those 7 routes all render the same `ProgramDashboard` placeholder (`App.tsx:633`). A learner is taught a loop the app can't run.
- **Short-term (S):** add a "concepts only — feature in development" badge to `safeCourse` in `frontend/src/data/academy/courses/safe.ts`; reconcile the duplicate catalog entry (`safeCourse` real vs `other-courses.ts` `safe-fundamentals` stub) so the marketplace shows one SAFe course.
- **Long-term (L):** ticket P2-SAFe below.
- **AC (short-term):** marketplace shows one SAFe course; its description no longer implies a working in-app PI board.

---

## P1 — High value, low effort (mostly NO migration — enforce stored data)

### P1-1 · Kanban: enforce WIP at pull · S · **no migration**
- `kanban/views.py:235-280` (`move`) → before reassigning `card.column`, compute `new_column.cards.exclude(pk=card.pk).count()`; if `>= wip_limit` return `Response({'detail': 'WIP limit reached'}, status=409)` **before any save**. Allow explicit `expedite` swimlane bypass.
- `KanbanBoard.tsx:50,62` → disable forward chevron + tooltip when target at limit (`toast.error(err.detail)` at `:24` already surfaces it).
- **AC:** moving a card into a full column returns 409 and the card does not move; expedite bypasses; chevron is disabled at limit.

### P1-2 · Scrum: gate "Done" on the Definition of Done · M · **no migration** (model exists, unwired)
- `scrum/views.py:186` (`BacklogItem.update_status`) → reject `→done` unless all required `DoDChecklistEntry.completed` for the sprint; set `Increment.meets_dod` (`models.py:251`) only when all sprint items pass.
- Add viewset + serializer for the orphaned `DoDChecklistEntry`/`DoDChecklistCompletion`; surface a checklist modal on the board "Done" action.
- **AC:** an item with unmet required DoD criteria cannot be set `done` (4xx + reason); `Increment.meets_dod` reflects real state.

### P1-3 · Waterfall: enforce the phase gate · M · **no migration**
- `waterfall/views.py:199-241` → block `complete` unless (a) all `is_required` deliverables `approved`, (b) Testing: all linked test cases `passed`; require `sign_off` of phase N before `start` of N+1. Surface blockers in `WaterfallPhaseGate.tsx:55-57`.
- **AC:** a phase with an unapproved required deliverable cannot complete; phase N+1 cannot start until N signed off.

### P1-4 · Waterfall: surface EVM (formulas already exist) · M · **no migration**
- `serializers.py:250-264` → expose `cpi/spi/cv/sv` (properties already on `WaterfallBudget.models.py:457-479`); add `EarnedValueRecordSerializer` + viewset + URL; derive PV/EV/AC from `WaterfallBudgetItem.planned_amount/actual_amount` × progress.
- `WaterfallBudget.tsx` + Overview → EVM card/trend; `AppSidebar.tsx:583` → "Earned Value" entry.
- **AC:** EVM card shows non-zero CPI/SPI derived from real budget+progress (not hand-entered).

### P1-5 · LSS Green: tollgate becomes a gate · M · **no migration** (reuses existing fields)
- `sixsigma/views.py:670-680` (`approve`) → add `can_pass()` precondition: Define→charter approved & SIPOC & ≥1 CTQ; Measure→baseline + data-collection plan; Analyze→≥1 validated root cause; Improve→pilot + before/after; Control→control plan + chart. Block next-phase POSTs until prior tollgate `passed`. Reuse `checklist_completed`/`deliverables_reviewed`.
- **AC:** approving a tollgate with missing deliverables is rejected with the specific missing item; next phase blocked until prior passes.

### P1-6 · LSS Black: enforce the shared tollgate · M · **no migration**
- `DMAICPhaseViewSet.update` (shared by Green & Black) + `LSSBlackPhases.tsx:80` → block `DMAICPhase → completed` unless its `sixsigma.TollgateReview` is `approved`/`passed`.
- **AC:** a Black phase cannot complete without an approved tollgate.

### P1-7 · LSS Black: remove duplicate sidebar item · S · **no migration**
- `AppSidebar.tsx:449` vs `:492` → collapse the duplicate "Hypothesis Tests" entry.
- **AC:** one Hypothesis Tests entry in the LSS-Black nav.

### P1-8 · Scrum: surface burndown + backlog reorder UI · S · **no migration**
- `scrum/views.py:386` (`record_burndown` populated, 0 frontend hits) → render `SprintBurndown` on the Sprint Board; auto-call on status change.
- `ScrumBacklog.tsx` → drag-handle / up-down calling the existing `items/reorder` route.
- **AC:** burndown chart renders from real data; PO can reorder the backlog from the UI.

### P1-9 · MSP: fix `_verify_program_access` scope bug · S · **no migration**
- `msp/views.py:18` → query `Program` not `Project` (FK + `get_queryset` filter on `Program`).
- **AC:** nested benefit/tranche create succeeds for a valid program; cross-tenant denied. (Hand to data-leak-hunter.)

### P1-10 · P2-Programme: fix tenant-scope bug · S · **no migration**
- `p2_programme/views.py:5,18` → repoint `_verify_programme_access` from `projects.Project` to `programs.Program` (FK target is `Program`, `models.py:17,56`).
- **AC:** programme-scoped access checks resolve against the correct table; cross-tenant denied. (Prereq for any P2-Programme redesign.)

### P1-11 · Hybrid: constrain methodology + reconcile UI lists · S · **no migration**
- `hybrid/serializers.py:19-23` → add `choices` to `methodology`; reconcile `HybridConfiguration.tsx:24` (10 values) vs `HybridPhases.tsx:25` (7 values).
- **AC:** invalid methodology rejected; both frontend lists match. (Prereq for P2-Hybrid binding.)

---

## P2 — Structural redesign (new models / larger flows — all 🔒)

### P2-SAFe · Executable PI loop 🔒 · L
- `safe/models.py` → `Feature`(FK `program_increment`,`art`,`wsjf_*`,state) · `Story`(FK `feature`,FK `programs.ProgramTeam`) · `Dependency`(source/target feature, ROAM) · `SystemDemo` · `InspectAdapt`. FK `PIObjective.team`→`ProgramTeam`; BV roll-up (planned vs achieved) on `ProgramIncrement`.
- Replace the shared `ProgramDashboard` shell (`App.tsx:633-667`) with real PI Planning board / Features kanban / Program Board / System Demo / I&A pages.
- WSJF = `(business_value+time_criticality+risk_reduction)/job_size`, orders the backlog.
- **AC:** an RTE can plan a PI, decompose features→stories to teams, ROAM a dependency, run a System Demo, and see BV roll-up; the `safe/` API has real frontend consumers.

### P2-MSP · Unify benefits + Blueprint + tranche loop 🔒 · L
- **Benefits (flagship):** pick one model (extend `msp.MSPBenefit`); add `baseline_value` (only a baseline *date* exists today → variance uncomputable); point `ProgramBenefits.tsx:19` at `/api/v1/msp/programs/{id}/benefits/` for MSP programmes; align field names (deprecate `programs.ProgramBenefit` for MSP).
- 🔒 `MSPBenefitDependency` (benefits map) + profile fields · 🔒 `MSPBlueprint` (POTI) + Vision on `Program` · 🔒 tranche loop with `@action close_tranche` (requires measurements + boundary review) · 🔒 `MSPTransition` + BCM role · realization cadence + roll-up.
- **AC:** a benefit shows actual-vs-baseline variance in the UI; a tranche can't close without logged measurements; Blueprint page replaces the `ProgramDashboard` alias (`App.tsx:670`).

### P2-PMI · Gate decisions over components 🔒 · L
- 🔒 `PMIGateDecision`(board→component, authorize/continue/hold/stop, gate, rationale, decided_at) + `authorize_component`/`stop_component` actions that mutate `PMIComponent.status` **only** via a recorded decision (block raw PATCH, `views.py:22`).
- 🔒 `PMIProgramCharter` · 🔒 `PMIBenefit` (identified→sustained) + component→benefit link · 🔒 `PMIStakeholder` (power/interest) · 🔒 Roadmap + life-cycle phase.
- Wire PMI to the UI (stranded backend) or consolidate on `programs`.
- **AC:** a component's status changes only through a recorded gate decision; charter/benefits/stakeholder pages read `/api/v1/pmi/...`.

### P2-P2-Programme · Programme board + Blueprint enforcement 🔒 · L
- 🔒 `P2ProgrammeBoardDecision`(FK Program + ProgrammeProject, continue/stop/defer) + `authorize`/`stop` actions as the **only** path to `status=approved/active` (lock direct PATCH).
- 🔒 require `blueprint` on authorize + `P2BlueprintCapability` sub-records · roll-up action · mandate/brief + tranche/dossier.
- **Share Blueprint/benefits/tranche with MSP — do not build a second copy.**
- **AC:** a proposed project can only go active via a board decision; Blueprint required to authorize; status rolls up to the programme dashboard.

### P2-Hybrid · Make PhaseMethodology a real strategy 🔒 · L
- 🔒 strategy resolver / `PhaseGate` so each phase **mounts the matching backend's flow** (scrum→sprint board/DoD, waterfall→gate/sign-off, kanban→WIP flow); sidebar reads per-phase methodology instead of the static list (`AppSidebar.tsx:623`).
- 🔒 `hybrid_phase_gate` (predictive sign-off / adaptive DoD-driven progress — closes the "mark complete = PATCH 100" hole, `HybridPhases.tsx:71-72`) · 🔒 entry/exit-criteria + predecessor for transitions · methodology-aware roll-up.
- **AC:** a "scrum phase" surfaces sprint mechanics and a "waterfall phase" surfaces a gate; they are no longer the same flat object; predictive phases require sign-off to advance.

### P2-Hybrid-Programme · Make config drive authorization 🔒 · L
- 🔒 `governance_mode` per constituent (gate/cadence/blend) + `ConstituentAuthorization`(FK Program + Project) whose `@action authorize` branches on the config (predictive→stage-gate, adaptive→cadence checkpoint).
- roll-up `@action` + a real `ProgramHybridGovernance.tsx` tab · make `HybridAdaptation` mutate the active config (not free text) · config-driven board · sidebar reads `Program.methodology` (replace hardcoded `'hybrid'`, `AppSidebar.tsx:966`).
- **AC:** two contrasting configs produce different authorization behaviour at runtime; constituents roll up to one programme view.

### P2-Scrum · Real Scrum events 🔒 · M
- 🔒 `ProductGoal`(backlog O2O) + `BacklogItem.product_goal` FK · 🔒 `RetroActionItem` surfaced in next Planning · redesign Sprint Planning into PBI-select + Sprint-Goal commit (writes `SprintGoal`) · Sprint Review accept/reject spawning follow-up items.
- **AC:** items trace to a Product Goal; retro actions carry forward; Planning commits a Sprint Goal + points.

### P2-Agile · Differentiate from Scrum 🔒 · M
- **Decision:** differentiate (don't consolidate — live customer on these routes). 🔒 `AgileDodEntry` gate · workboard with in-place assignment + status transitions (assignee exists, no UI) · 🔒 Goal→Epic→Item FKs · lean into outcome metrics + continuous-flow cadence (WIP) as the distinctive value.
- **AC:** Agile delivers something Scrum doesn't (flow/outcome metrics); DoD gates done; team can assign on the board. *(If not pursued next quarter, the honest call is to deprecate `agile/` and route to `scrum/`.)*

### P2-Kanban · Classes of service + policies + flow 🔒 · M
- 🔒 `class_of_service` on `KanbanCard` (expedite bypasses WIP, sorts top, flags SLE breach) · 🔒 column-scoped `WorkPolicy` rendered at column headers · 🔒 `blocked_at`/`BlockEvent` for aging-WIP + blocked-time tiles · 🔒 kaizen action + cadence model.
- **AC:** expedite items bypass WIP and surface; column policies show inline; aging/blocked WIP appears in Flow Metrics.

### P2-LSS · One DMAIC spine, compute capability 🔒 · M
- Capability: `compute_capability` action (Cp/Cpk/sigma/DPMO from real measurements) · collapse the 3 duplicate metric stores onto `sixsigma`.
- Black: 🔒 FK advanced tools to the decision spine (`HypothesisTest.root_cause`→Fishbone, `DOE.improves_solution`→Solution, `SPCChart↔ControlPlan`); retire duplicate `lss_black` Hypothesis/SPC/ControlPlan (keep DOE as Black-only); SPC special-cause rule engine; 🔒 Green-Belt mentoring link; 🔒 savings-validation ledger + Champion sign-off.
- **AC:** Cp/Cpk computed from data; one canonical backend per tool; SPC flags out-of-control points and triggers the reaction plan.

### P2-Governance · Binding decisions + funded portfolio 🔒 · M
- 🔒 `DecisionVote` + `quorum` on `GovernanceBoard` (enforce before P0-1 `apply`; append-only once approved) · 🔒 Portfolio `priority`/`rank` + `ComponentFunding`(portfolio, program/project, amount, fiscal_period) · stakeholder engagement tracking (matrix already exists).
- **AC:** a decision can't apply without quorum; a portfolio can rank + fund components against capacity.

---

## Academy training backlog (sequence AFTER the matching product is operable)

| # | Item | Why | Gate |
|---|---|---|---|
| T-1 | Reconcile duplicate SAFe catalog entry | `safeCourse` real + `safe-fundamentals` stub both show | Do now (P0-3) |
| T-2 | Author **LSS Black Belt** course | Product tooling already exists to demo against; current Academy stops at Green Belt | Ready now |
| T-3 | Promote **MSP / Hybrid / PMI** stubs to real courses | They're catalog placeholders today | **Only after** P2-MSP / P2-Hybrid / P2-PMI ship — avoid new SAFe-style mismatches |
| T-4 | Add **PRINCE2 Programme** + **Hybrid Programme** courses | No course exists | After products operable |

---

## Suggested execution order
1. **P1 no-migration enforcement** (P1-1…P1-11) — fastest credibility wins, mostly ½–2d each, no backup gate. Start with Kanban WIP (P1-1) and the two scope bugs (P1-9, P1-10).
2. **P0 governance core** (P0-1, P0-2) — unblocks the program boards; first migration → **run data-guardian first**.
3. **P0-3 SAFe course badge** in parallel (docs-only).
4. **P2 redesigns** one methodology at a time, each behind a data-guardian backup, each with a regression scenario added to `tests/e2e/` (mirror the WP↔Task roll-up pattern, commit `e4868761`).

> Every 🔒 ticket: **data-guardian backup before `makemigrations`/migrate**; never migrate production from the MacBook; add a regression test per shipped gate.
