stages:
  - test

typecheck:mobile:
  stage: test
  image: node:20
  cache:
    key: mobile-${CI_COMMIT_REF_SLUG}
    paths:
      - node_modules/
  script:
    - npm ci
    - npx tsc --noEmit
  only:
    - main
    - merge_requests
