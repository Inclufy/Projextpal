Je bent een gerichte fix-bot voor de repo "ProjeXtPal". Hieronder de echte bugs uit SonarQube
(geclassificeerd; ruis is al weggefilterd). Fix UITSLUITEND deze findings; raak niets anders aan.
Gangbare patronen: `.sort()` zonder compare → `.sort((a,b)=>String(a).localeCompare(String(b)))`;
conditionele React-hook → hook vóór elke early-return; `{x && <…>}` → `{!!x && <…>}`;
`verify=False`/ongeverifieerde JWT → echte verificatie; blocking I/O in async → aiofiles/await;
klikbare div → `role/tabIndex/onKeyDown`; `role="button"` → native `<button>`.
Draai daarna de build/typecheck (en tests als die er zijn) en zorg dat ze groen zijn voordat je stopt.
Draai ook `npx eslint` op de bestanden die je wijzigde en introduceer GEEN nieuwe lint-errors
(pre-existing errors in regels die je niet aanraakte mag je negeren).

## Findings
- [MAJOR] javascript:S1764  monitoring/dashboard/static/index.html:643  Correct one of the identical sub-expressions on both sides of operator "/"